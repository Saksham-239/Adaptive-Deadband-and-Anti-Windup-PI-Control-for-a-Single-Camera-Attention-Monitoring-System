# ============================================================
# Adaptive AI Study Attention System — Requirements
# ============================================================
# IMPORTANT: Install PyTorch CUDA FIRST (before this file).
# Run:
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install -r requirements.txt
#
# Validate GPU:
#   python -c "import torch; print(torch.cuda.is_available())"  # must be True
#   python -c "from ultralytics import YOLO; m=YOLO('yolov8n.pt'); print(m.device)"
# ============================================================

# Core vision
# NOTE: mediapipe GPU delegate on Windows may not be available — test on Day 1.
# If CPU-only, drop camera resolution to 640x480 in config.yaml.
mediapipe==0.10.14
opencv-python==4.10.0.84

# Object detection — torch installed separately above (CUDA wheel)
ultralytics==8.3.40

# Numerics — must match mediapipe's constraint
numpy==1.26.4

# Config
pyyaml==6.0.2

# Audio (TTS via SAPI5 on Windows)
pyttsx3==2.98

# Dashboard
streamlit==1.39.0
plotly==5.24.1

# ML (v2 scope — safe to install now, used later)
scikit-learn==1.5.2
xgboost==2.1.3

# Testing
pytest==8.3.3
